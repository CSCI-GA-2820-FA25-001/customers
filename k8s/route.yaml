apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: customers-service
  labels:
    app: customers
spec:
  to:
    kind: Service
    name: customers-service
    weight: 100
  port:
    targetPort: 8080       
  tls:
    termination: edge
  wildcardPolicy: None
